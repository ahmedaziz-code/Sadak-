{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Labell</th>
                <td>{{ category.labell }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_con_index') }}">back to list</a>

    <a href="{{ path('category_con_edit', {'id': category.id}) }}">edit</a>

    {{ include('category_con/_delete_form.html.twig') }}
{% endblock %}

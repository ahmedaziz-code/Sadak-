<form method="post" action="{{ path('produit_delete', {'refProduit': produit.refProduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.refProduit) }}">
    <button class="btnn">Delete</button>
</form>
